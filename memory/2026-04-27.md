### Linux-Windows WSL Samba Share Setup (2026-04-27)

Configured Samba share for `/srv/github` with the following details:
- **Path:** `/srv/github`
- **Permissions:** Read/write
- **Restricted to users:** `leto`, `sienna`
- **Force group:** `openclaw`
- **Config block:**
  ```
  [github]
    path = /srv/github
    browseable = yes
    writable = yes
    valid users = leto, sienna
    force user = leto
    force group = openclaw
    create mask = 0775
    directory mask = 0775
  ```

Steps followed:
1. Updated `/etc/samba/smb.conf` with the configuration.
2. Restarted Samba service: `sudo systemctl restart smbd`.
3. Added Samba users `leto` and `sienna` with `smbpasswd` commands.
4. Verified share availability from Windows/WSL at `\\<server-ip>\github`.

**Outcome:** Share is working correctly. User will set up Kira next.