- ChoreBoss frontend was decomposed one level deeper again.
- Added `useChoreBossDashboardData` to own chore/people loading and the people list state.
- Added `useChoreBossPeopleManagement` to isolate person CRUD/form state from dashboard loading.
- Added `useChoreBossChoreCompletion` to isolate chore completion state and action.
- `useChoreBossDashboard` now composes the data hook + people management hook + chore completion hook.
- The current TS frontend direction is now: `App` -> `ChoreBossScreen` -> `useChoreBossApp` -> `useChoreBossAuth` + `useChoreBossDashboard` -> `useChoreBossDashboardData` + `useChoreBossPeopleManagement` + `useChoreBossChoreCompletion`.
- Added an `assign_chores` flag to ChoreBoss people so some household members can be excluded from rotation.
- Wired `assign_chores` through the FastAPI person schema/router, legacy Flask bridge, legacy Flask routes, TS frontend forms/types, and the rotation lookup.
- Added an Alembic migration to add `people.assign_chores` with a default of `true`.
- Added backend coverage for the new behavior, including a rotation test that skips unchecked people.
- Updated the legacy /change_sequence view to show each person row's assignment state with a right-aligned checkbox and `data-assign-chores` marker.
- The change-sequence checkbox is now toggleable on the live Flask bridge and immediately persists to the backend via the person update route.
- The live Flask bridge now shows the assignment checkbox state in the Change Sequence page after a restart with template caching cleared.
