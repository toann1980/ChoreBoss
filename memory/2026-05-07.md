- ChoreBoss UI should always expose a Home button on non-home screens and Logout when signed in.
- The shared Flask back-button partial was trimmed so it no longer duplicates Home now that the base shell renders global Home/Logout controls.
- The legacy Edit People page now routes Change Sequence to the actual sequence screen instead of looping back to the people list.
- The legacy chore detail view now rehydrates assigned/last-completed people so the assigned person is visible again.
- The legacy Flask base template carries shared Home/Logout affordances where possible; the TS shell mirrors that in `AppLayout`.
- New backlog items: restore the user sort-order / sequence view, and add an explicit disable-assignment control for chores that should stay unassigned (including test-user scenarios).


## Session snapshot — 2026-05-07 (PDT)

Timestamp: 2026-05-07 17:33 PDT

Context: Interactive DEV session working on ChoreBoss (Flask bridge + FastAPI backend). Tasks focused on PIN verification, UI numpad ergonomics, permission model, sequence updates, and UI consistency (Back/Home/Logout).

Actions taken (durable changes I made):

- Auth & users:
  - Normalized login_name handling (case-insensitive) at API boundary and normalized existing DB values. Backup created: /srv/github/ChoreBoss/choreboss.db.bak.1778188460
  - Confirmed people repository/service uses bcrypt for PIN hashing and verification; no decryption of PINs occurs.
  - Promoted account "dad" (login_name normalized to "dad") to is_admin = true on request (DB updated in-place).

- PIN flow & UI:
  - Increased numpad button sizes and spacing in web/static/css/styles.css to improve touch targets.
  - Reworked pin_modal.html JS to store modal-local pin value (modalPin) and perform defensive checks; copy into page-level hidden inputs only when needed.
  - Made bridge verify responses clearer (added reason field on failure) and made frontend accept multiple success shapes (status:'success' or success:true) to avoid regressions.
  - Fixed complete chore flow: form submit now redirects or renders friendly pages instead of showing raw JSON.

- Bridge & backend behavior:
  - Added /people/sequence endpoint on FastAPI to accept sequence updates (admin-only). Bridge now proxies update_sequence to this endpoint and returns consistent success response.
  - Restarted both Flask bridge and FastAPI processes multiple times during iteration; validated health endpoints on ports 8055 (backend) and 8056 (bridge).
  - Restarted services on user request and killed/replaced stale processes when port 8056 was unresponsive.

- Template/UI consistency:
  - Centralized Back button into the header in web/templates/base.html (upper-left). Home + Logout are upper-right when session present.
  - Removed per-page Back buttons across templates (edit_chore, edit_person, edit_pin, add_chore, add_person, chore_detail, change_sequence, edit_people) to avoid duplicates; left helper partial in repo initially but later recommended removal.
  - Committed changes to development branch, then created and pushed main branch from development; pushed to origin/main.
  - Deployed updates to running bridge so changes are live (hard-refresh required on clients).

- Dev workflow & repo updates:
  - Committed changes with message referencing UI consistency and pin modal fixes; pushed to origin/development then mirrored to origin/main.
  - Noted GitHub Dependabot advisories surfaced by push (informational).

- Observations & QA notes:
  - Found a response-shape mismatch for sequence update (bridge returned success:true while frontend expected status:'success'), fixed both sides.
  - Observed intermittent UI showing "Invalid PIN" due to cached JS/CSS and earlier permission mismatch (dad was not admin initially). After promoting account and restarting bridge, flows worked.
  - Verified sequence updates apply in DB even when UI reported failure; adjusted frontend to accept both response shapes.

Open TODO / follow-ups (recommended & recorded):

- Add WebAuthn (FIDO2) biometric login as an MVP: endpoints for register/authenticate, frontend navigator.credentials flows, DB table for credentials. (Estimate 2–5 days for robust MVP.)
- Remove web/templates/back_home_buttons.html to prevent accidental reintroduction of page-level Back buttons.
- Add automated check (scripts/check-back-buttons.sh) and a CI job to prevent reintroducing per-page Back buttons.
- Replace alert() UX with non-blocking toasts and inline DOM updates for better UX on sequence/complete flows.
- Add audit logs for admin-only actions (who changed sequence, when; who completed chores and when) and failed auth attempts (no PIN values logged).
- Add unit/integration tests for /people/sequence endpoint and main auth flows.

Additional requested backlog items:
- Login PIN field should pop up a numeric keypad for number-only entry. ✅ Implemented and smoke-tested on the FastAPI login page.
- Back button should refresh/reload state so users don’t see stale sequence/order data after navigating back.
- Finish/validate add/edit chore admin flows.
- User view should show assigned chores and when they are due.
- Add an admin approval path for chore completion requests so a user can request approval and the admin can approve/cycle the chore.

Requested by: Toan (session)

Notes: No plaintext PINs were written to disk or logs. All changes were read/write to application code and DB backups as requested by the user during the session.

-- Nova


## Baton pass completion — 2026-05-07 21:26 PDT

- The FastAPI shell migration is now functionally complete for the current ChoreBoss UI: login, dashboard, chores, people, sequence, verify-pin, edit pin, and add pages render directly from FastAPI.
- `start_frontend.sh` now launches `run.py` instead of the Flask bridge.
- `flask_bridge.py` was removed from the working tree and its process was stopped.
- I restored the SQLite database from `choreboss.db.bak.1778188460` after discovering the live DB file had been empty, then re-promoted `dad` to admin so admin-only smoke checks could pass.
- Smoke tests passed on `http://127.0.0.1:8055` for:
  - GET `/login`
  - POST `/login`
  - GET `/`
  - GET `/chores`
  - GET `/people`
  - GET `/chores/add`
  - GET `/people/add`
  - GET `/people/5/edit_pin`
  - POST `/verify_pin`
  - POST `/update_sequence`
  - GET `/chores/6`
  - GET `/people/5`
- The live backend health check remained green after the cutover.
- Remaining follow-up: clean up or rewrite bridge-specific docs and sync the memory notes with the final cutover state.
