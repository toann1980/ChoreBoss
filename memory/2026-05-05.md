## 2026-05-05 18:51 PDT — BenchModel extraction path fixed for reasoning models

- Updated BenchModel so runs preserve whether the useful text came from `content` or `reasoning_content`.
- The runners now re-parse the stored text using the original response field instead of always wrapping it as `content`.
- This should prevent reasoning-model outputs from being misclassified as extraction failures when the useful text lives in `reasoning_content`.
- Added regression coverage for the round-trip behavior in `tests/test_response_parser.py`.
