## 2026-05-05 18:28 PDT — BenchModel runner probe + clean rerun

- Probed the BenchModel runner behavior on kids-01.
- Finding: the CLI `benchmodel.py run` walks **all available models** from the hot-swap API, so the earlier run looked stalled because it was doing a broad sequential loop, not because DeepSeek-R1-distill was hung.
- Reran a **clean isolated single-model benchmark** using `BenchmarkRunner.run_all([model], scenarios)` against `DeepSeek-R1-Distill-Qwen-14B-Q5_K_M_bartowski`.
- Outcome: all 9 measurements succeeded; the model remained responsive.
- Summary: ~20.9s average latency, ~9.6 tok/s, 0% extraction on the free-form envestero scenarios.
